public abstract class Automobile {
    // this class does not have to be abstract but lets make it abstract
    // public abstract class ... 
    // we cannot create any automobile objects its too generic 
    

    // lets declare an abstract method that doesnt return anything
    // if your a automobile you need a go method 
    // so what we are going to do is create a few diff types of automobiles which will inherit from the class automobile 
    abstract void go(); 



    
}